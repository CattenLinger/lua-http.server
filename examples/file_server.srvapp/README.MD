File Server
===
A suit to bootstrap a file server under root directory

Features
---
- Show index.html if it's under a directory.
- List files if it's a directory.
- Click on file link to download a file.

It uses lua template to render pages. 
change templates under `pages/` to change how it looks.