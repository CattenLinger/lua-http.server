#!/usr/bin/env sh
set -e
export LUA_HTTP_SERVER_HOME_DIR=$(realpath $(dirname $(realpath "$0"))"/..")

LUA_DIR="$LUA_HTTP_SERVER_HOME_DIR/lua"
exec lua -e "package.homedir=[[$LUA_HTTP_SERVER_HOME_DIR]]" "$LUA_DIR/init.lua" $@